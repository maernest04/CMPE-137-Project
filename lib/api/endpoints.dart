// Centralizing all API paths so strings in widgets aren't hardcoded
class ApiEndpoints {
  static const studySpaces = '/study-spaces';
  static const savedSpaces = '/users/me/saved-spaces';
  static const profile = '/users/me';
}